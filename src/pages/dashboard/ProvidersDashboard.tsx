import React from "react";
import { Navigate } from "react-router-dom";

const ProvidersDashboard = () => {
  return <Navigate to="/dashboard/providers/crm" replace />;
};

export default ProvidersDashboard;
